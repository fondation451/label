import { CourtDecisionService } from './court-decision.service';

describe('CourtDecisionService', () => {
  it('should be defined', () => {
    expect(new CourtDecisionService()).toBeDefined();
  });
});
